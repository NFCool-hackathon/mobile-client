import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'xyz.nfcool.app',
  appName: 'nf-cool-client-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
